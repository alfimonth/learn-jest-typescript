describe('Data type', () => {
    it('should must declare ', () => {
        let name: string = 'hanif daffa';
        let balance: number = 1000;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});